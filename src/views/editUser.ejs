<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Latest compiled and minified CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Latest compiled JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Edit user</title>
</head>

<body>
    <div class="user-infor-container mt-3">
        <div class="container">
            <div class="row">
                <div class="loginForm-user mt-3">
                    <div class="">
                        <h3>Edit user</h3>
                    </div>
                        <form method="post" action="/postConfirmEditUser">
                            <input name = "id" value = "<%= results.id %>" hidden>
                            <div class="mb-2 col-6">
                                <label for="email" class="form-label">Email address</label>
                                <input type="email" class="form-control" id="email" aria-describedby="emailHelp"
                                    name="email" value="<%= results.email %>" required>
                            </div>
                            <div class="mb-2 col-6">
                                <label for="username" class="form-label">Username</label>
                                <input type="text" class="form-control" id="username" name="username" value="<%= results.username %>" required>
                            </div>
                            <button type="submit" class="btn btn-primary">Confirm</button>
                        </form>
                </div>
            </div>
        </div>
    </div>

</body>

</html>